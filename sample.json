[
    {
        "name": "stop non-prod apps",
        "schedule": {
            "frequency": "1h"
        },
        "filters": {
            "organization": "Org.Name != 'system'",
            "space": "Space.Name != 'prod'",
            "application": "App.State == 'STARTED' && TimeSince(App.UpdatedAt) < TimeParseDuration('1H')",
            "action": "stop"
        }
    },
    {
        "name": "delete stopped non-prod apps",
        "schedule": {
            "frequency": "1h"
        },
        "filters": {
            "organization": "Org.Name != 'system'",
            "space": "Space.Name != 'prod'",
            "application": "App.State == 'STOPPED' && TimeSince(App.UpdatedAt) < TimeParseDuration('3H')",
            "action": "delete"
        }
    }
] 